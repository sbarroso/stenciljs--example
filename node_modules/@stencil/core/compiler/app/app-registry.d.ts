import { AppRegistry, BuildConfig, BuildContext } from '../../util/interfaces';
export declare function generateAppRegistry(config: BuildConfig, ctx: BuildContext, appRegistry: AppRegistry): Promise<void>;
